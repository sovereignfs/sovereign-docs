import { EmptyState } from '@sovereignfs/ui';

export default function SovereignDocsIndexPage() {
  return (
    <EmptyState
      heading="Sovereign Docs is under construction"
      description="The document workspace lands in a later task — see roadmap.md."
    />
  );
}
